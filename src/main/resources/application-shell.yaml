spring:
  main:
    banner-mode: OFF
logging:
  level:
    io:
      github:
        hider:
          mongoway: INFO
    org:
      mongodb:
        driver: ERROR
      springframework:
        boot: OFF
        data:
          mongodb: ERROR
  pattern:
    console: "%msg%n"
